# secrets -- .env.example is the committed template, .env never is
.env
.env.*
!.env.example

# generated output (seed/ is committed on purpose -- see DEPLOY.md)
library/
.paper2vid/
tests/_out/
*.mp4

# python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# editors / os
.vscode/
.idea/
.DS_Store

# build output written to the repo root by the CLI
/*.html

# runtime state, never commit -- contains visitor ids and emails
seed/_*.json
